# Heart-Disease-Prediction
A binary classification project using the UCI Heart Disease dataset to predict the risk of heart disease. Includes data cleaning, EDA, model training (Logistic Regression, Random Forest, SVM), SMOTE for class imbalance, and performance evaluation using accuracy, ROC-AUC, and confusion matrix.
